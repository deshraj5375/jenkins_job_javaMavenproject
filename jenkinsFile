node()
{

mvn=tool 'm3'
stage('checkout')
{
    sh 'rm -rf *'
    checkout scm
}

stage('Complie and package')
{
 sh 'mvn clean package'
}

stage('build docker file')
{
sh '''
 mkdir dockerimage
 cd dockerimage
 cp $WORKSPACE/target/*.jar .
 '''
}

}
